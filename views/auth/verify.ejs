<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Verify OTP - Royal Panda</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-red-100 min-h-screen flex flex-col">
  <%- include('../partials/nav', { cartCount: 0 }) %>

  <div class="flex-grow flex items-center justify-center py-12">
    <div class="bg-white shadow-2xl rounded-xl p-8 w-96">
      <h2 class="text-2xl font-bold text-red-600 text-center mb-6">Verify OTP</h2>
      <form action="/auth/verify-otp" method="POST" class="flex flex-col space-y-4">
        <input type="hidden" name="mobile" value="<%= mobile %>" />

        <input 
          type="text" 
          name="otp" 
          placeholder="Enter 6-digit OTP"
          maxlength="6"
          required
          class="p-3 border border-red-300 rounded-md focus:outline-none focus:ring-2 focus:ring-red-400"
        />

        <button 
          type="submit"
          class="bg-green-600 text-white p-3 rounded-md hover:bg-green-700 transition font-medium"
        >
          Verify OTP
        </button>
      </form>
    </div>
  </div>
</body>
</html>
