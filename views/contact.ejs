<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Contact Us - Royal Panda</title>
  <script src="https://cdn.tailwindcss.com"></script>

  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            'royal-panda-red': '#E11D48',
            'royal-gray': '#f8f8f8',
          },
        },
      },
    };
  </script>

  <style>
    body {
      background: radial-gradient(circle at top left, #fff 0%, #f3f4f6 50%, #e5e7eb 100%);
      font-family: 'Inter', sans-serif;
    }
    .glass {
      backdrop-filter: blur(18px);
      background: rgba(255, 255, 255, 0.75);
    }
    .input-focus:focus {
      border-color: #E11D48;
      box-shadow: 0 0 0 3px rgba(225, 29, 72, 0.25);
      transition: all 0.15s ease-in-out;
    }
  </style>
</head>

<body class="min-h-screen flex flex-col text-gray-800">
  <!-- Navbar -->
  <%- include('./partials/nav') %>

  <!-- Main Section -->
  <main class="flex-grow flex items-center justify-center px-6 py-12">
    <div class="glass border border-gray-200 rounded-2xl shadow-2xl max-w-2xl w-full p-8">

      <!-- Header -->
      <div class="text-center mb-8">
        <h1 class="text-4xl font-extrabold text-gray-900 tracking-tight">
          📞 Contact <span class="text-royal-panda-red">Royal Panda</span>
        </h1>
        <p class="text-gray-500 mt-2 text-sm">
          Have questions, feedback, or need assistance? We’d love to hear from you!
        </p>
      </div>

      <!-- Contact Form -->
      <form action="/contact" method="POST" class="space-y-6">
        <!-- Name -->
        <div>
          <label for="name" class="block text-sm font-semibold text-gray-700 mb-1">Full Name</label>
          <input type="text" id="name" name="name" placeholder="Enter your full name" required
            class="w-full border border-gray-300 rounded-lg px-4 py-2 input-focus bg-white focus:outline-none" />
        </div>

        <!-- Mobile -->
        <div>
          <label for="mobile" class="block text-sm font-semibold text-gray-700 mb-1">Mobile Number</label>
          <input type="tel" id="mobile" name="mobile" placeholder="Enter your mobile number" required pattern="[0-9]{10}"
            class="w-full border border-gray-300 rounded-lg px-4 py-2 input-focus bg-white focus:outline-none" />
        </div>

        <!-- Email -->
        <div>
          <label for="email" class="block text-sm font-semibold text-gray-700 mb-1">Email Address</label>
          <input type="email" id="email" name="email" placeholder="example@royalpanda.com" required
            class="w-full border border-gray-300 rounded-lg px-4 py-2 input-focus bg-white focus:outline-none" />
        </div>

        <!-- Message -->
        <div>
          <label for="message" class="block text-sm font-semibold text-gray-700 mb-1">Your Message</label>
          <textarea id="message" name="message" rows="4" required
            placeholder="Tell us how we can help..."
            class="w-full border border-gray-300 rounded-lg px-4 py-2 input-focus bg-white resize-none focus:outline-none"></textarea>
        </div>

        <!-- Submit Button -->
        <div class="pt-2">
          <button type="submit"
            class="w-full bg-royal-panda-red text-white font-semibold rounded-lg py-3 text-lg shadow-md hover:bg-red-700 transition-all duration-200 hover:shadow-lg">
            🚀 Send Message
          </button>
        </div>
      </form>

      <!-- Footer Section -->
      <div class="text-center mt-8 text-gray-500 text-sm">
        <p>📍 Royal Panda HQ – 123 Food Street, New Delhi, India</p>
        <p class="mt-1">📧 support@royalpanda.com | ☎️ +91 98765 43210</p>
      </div>
    </div>
  </main>

  <footer class="bg-royal-panda-red text-white text-center py-4 mt-8">
    <p class="text-sm">&copy; <%= new Date().getFullYear() %> Royal Panda. All rights reserved.</p>
  </footer>
</body>
</html>
